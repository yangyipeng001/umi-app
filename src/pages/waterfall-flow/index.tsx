import s from './index.scss';

const WaterfallFlow = () => {
  return (
    <div className={s.waterfallFlowWrap}>
      <div className={s.title}>瀑布流</div>
    </div>
  );
};

export default WaterfallFlow;
